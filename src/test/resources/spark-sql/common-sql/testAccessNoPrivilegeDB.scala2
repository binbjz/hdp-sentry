scala> val test_sql="USE default";
test_sql: String = USE default

scala> spark.sql(test_sql).collect().foreach(println);

scala>

scala> val test_sql="SHOW DATABASES LIKE 'unaccessibledb'";
test_sql: String = SHOW DATABASES LIKE 'unaccessibledb'

scala> spark.sql(test_sql).collect().foreach(println);
[unaccessibledb]